import React from "react";

const imageDrawer = (props) => (
    <img
        src={props.linkImage}
        style={{ width: 'auto', height: 45, marginLeft: 15 }} alt="Logo Company" />
);

export default imageDrawer;

